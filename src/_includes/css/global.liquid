<style>
  * {
    padding: 0;
    margin: 0;
    line-height: calc(1em + 0.75rem);
  }
  html {
    --color-background: #f5efd7;
    --color-background-shade: #f0e2b5;
    --color-text: #301e1a;
    --color-text-shade: #614942;
    --color-primary: #FD7701; /* New orange color */
    
    /* RGB versions of colors for transparency */
    --color-text-rgb: 48, 30, 26;
    --color-text-shade-rgb: 97, 73, 66;

    /* Browser UI Color */
    --browser-ui-color: #f5efd7;
    
    --font-family: -apple-system, BlinkMacSystemFont, "Inter", "IBM Plex Sans",
      Segoe UI, Helvetica, Arial, sans-serif, Apple Color Emoji, Segoe UI Emoji,
      Segoe UI Symbol;

    --container-max-size: 75ch;
    --container-padding-sides: var(--space-7);

    --space-1: 0.35rem;
    --space-2: 0.5rem;
    --space-3: 1rem;
    --space-4: 1.5rem;
    --space-5: 2rem;
    --space-6: 2.5rem;
    --space-7: 3rem;
    --space-8: 4rem;
    --space-9: 5rem;
    --space-10: 6rem;

    --font-size-xs: clamp(0.8rem, 0.12vw + 0.8rem, 1rem);
    --font-size-base: clamp(0.9rem, 0.16vw + 0.9rem, 1.25rem);
    --font-size-lg: clamp(1.2rem, 0.12vw + 1.2rem, 1.6rem);
    --font-size-xl: clamp(1.6rem, 0.14vw + 1.6rem, 2rem);
    
    height: 100%;
    background-color: var(--color-background);
  }
  [data-theme="dark"] {
    --color-background: #0d0d0d;
    --color-background-shade: #181818;
    --color-text: #f8f5e9;
    --color-text-shade: #d6c8a6;
    --color-primary: #FD7701; /* Using the same orange for dark mode */
    
    /* Browser UI Color */
    --browser-ui-color: #0d0d0d;
    
    /* RGB versions of colors for transparency in dark mode */
    --color-text-rgb: 248, 245, 233;
    --color-text-shade-rgb: 214, 200, 166;
  }
  h1 {
    font-size: var(--font-size-xl);
  }
  h2 {
    font-size: var(--font-size-lg);
    color: var(--color-text-shade);
    font-weight: normal;
    margin-bottom: var(--space-2);
  }
  h3,
  h4,
  h5,
  h6 {
    font-size: var(--font-size-base);
  }
  body {
    font-family: var(--font-family);
    color: var(--color-text);
    background-color: var(--color-background);
    font-size: 1rem;
    font-size: var(--font-size-base);
    text-rendering: optimizeLegibility;
    min-height: 100%;
    /* Remove padding that might interfere with full bleed to edges */
    padding: 0;
    margin: 0;
    /* Allow content to flow into the safe areas but respect them for content */
    padding-left: env(safe-area-inset-left);
    padding-right: env(safe-area-inset-right);
  }
  a {
    color: var(--color-primary);
  }
  
  /* Add arrow to external links */
  a[href^="http"]:not([href*="hunsanger"]):after {
    content: " ↗";
    display: inline-block;
    font-family: system-ui, sans-serif;
    color: var(--color-text-shade);
    opacity: 0.7;
    font-size: 0.9em;
    vertical-align: super;
    font-size: 0.7em;
    margin-left: 2px;
  }
  
  /* Exclude arrows from navbar, footer, and special elements */
  .navbar a[href^="http"]:after,
  .footer a[href^="http"]:after,
  .nav-links a[href^="http"]:after,
  .dark-toggler-container a[href^="http"]:after,
  a.theme-toggle[href^="http"]:after,
  a.link-home[href^="http"]:after {
    content: none;
  }
  
  img,
  picture {
    height: auto;
    max-width: 100%;
    flex-shrink: 0;
  }
  blockquote {
    padding: 0 1em;
    color: var(--color-text-shade);
    border-left: 0.25em solid var(--color-primary);
    background-color: rgba(253, 119, 1, 0.05); /* Updated to match #FD7701 */
  }
  button {
    display: inline;
    background-color: var(--color-primary);
    color: white;
    border-radius: 4px;
    border: none;
    font-size: var(--font-size-base);
    padding: 0.5em 1em;
    font-size: inherit;
    line-height: 1.15;
    transition: background 0.2s ease-in-out;
    cursor: pointer;
  }
  button:hover {
    background-color: #e16a00; /* Darker shade of #FD7701 for hover */
    color: white;
  }
  [data-theme="dark"] button:hover {
    background-color: #ff8a2c; /* Lighter shade of #FD7701 for dark mode hover */
  }
  pre {
    padding: var(--space-4);
    border-radius: 4px;
    font-size: var(--font-size-xs);
    background: var(--color-background-shade);
    border-left: 3px solid var(--color-primary);
    --prism-background: var(--color-background-shade);
    --prism-font-size: var(--font-size-xs);
  }
  code {
    font-size: var(--font-size-xs);
    white-space: pre-wrap;
    word-spacing: normal;
    word-break: normal;
    word-wrap: normal;
    -moz-tab-size: 4;
    -o-tab-size: 4;
    tab-size: 4;

    -webkit-hyphens: none;
    -moz-hyphens: none;
    -ms-hyphens: none;
    hyphens: none;
  }

  .visually-hidden {
    clip: rect(0 0 0 0);
    clip-path: inset(50%);
    height: 1px;
    overflow: hidden;
    position: absolute;
    white-space: nowrap;
    width: 1px;
  }

  /* iOS/iPadOS specific adjustments for full-screen mode */
  @supports (padding: max(0px)) {
    body {
      /* This ensures the body takes up the full viewport */
      min-height: 100vh;
      min-height: -webkit-fill-available;
    }
    
    .navbar {
      padding-left: max(var(--container-padding-sides), env(safe-area-inset-left));
      padding-right: max(var(--container-padding-sides), env(safe-area-inset-right));
      /* Add extra padding at the top to account for the status bar */
      padding-top: calc(var(--space-7) + env(safe-area-inset-top));
    }
    
    .main-container {
      padding-left: max(var(--container-padding-sides), env(safe-area-inset-left));
      padding-right: max(var(--container-padding-sides), env(safe-area-inset-right));
      padding-bottom: max(var(--space-4), env(safe-area-inset-bottom));
    }
  }
  
  /* Mobile-specific adjustments for thinner margins */
  @media (max-width: 768px) {
    html {
      --container-padding-sides: var(--space-3);
    }
  }
</style>
